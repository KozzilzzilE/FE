feat(solver): 채점/실행 로직 개선 및 UI 업데이트

- 채점 진행 상태 및 에러 메시지 연동
  - `ProblemDto` 및 `SolverRepository`에서 `progress`, `message` 데이터 파싱 추가
  - `SolverViewModel` 폴링 로직에서 진행률과 에러 메시지를 `SubmitResult`에 반영하도록 수정
- 결과 화면 UI 개선 (`SubmitTabContent`)
  - 채점 진행 중(PROCESSING) 상태일 때 원형 로딩 스피너와 진행률(%)이 중앙에 표시되도록 추가
  - 정답 시 텅 빈 공간에 '사용 언어' 및 '제출 시간(date)'을 표시하여 사용성 개선
  - 실행 결과 및 제출 결과의 실행 시간(`runtimeMs`) 단위를 초(s)가 아닌 밀리초(ms)로 명시하여 표시하도록 수정
- 사용자 편의성 향상
  - `SolveScreen`에서 실행/제출 버튼 클릭 시 지연 없이 즉각적으로 해당 결과 탭으로 이동하도록 네비게이션 로직 수정
  - `SubmissionDetailScreen`에서 AI 리뷰 도착 완료 시 가시성 향상을 위해 체크 아이콘 추가
- 예외 상황 처리 강화
  - 런타임/컴파일 에러 시 상세 내역 출력을 위해 `RunResult`에 `stderr`, `compileOutput` 필드 확장
  - 테스트 케이스에 입력(input)이 누락될 경우 초기 `originalInput` 데이터를 사용하도록 보강
